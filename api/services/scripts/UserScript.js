var UserService;
UserService = {};
UserService.methods = (function () {
    this.getUserIdFromUserEmail = (function () {
        var promise = new Promise(function (resolve, reject) {
            resolve(1);
        });
    });
});

module.exports(UserService);
