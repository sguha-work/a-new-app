var PriceService;
PriceService = {};

PriceService.methods = (function () {
    this.getPriceTypeIdFromTitle = (() => {
        var promise = new Promise((resolve, reject) => {
            resolve(1);
        });
        return promise;
    });
});
